// exemplo de operadores lógicos e estruturas condicionais
lado = input ("Informe a medida do lado de um quadrado: ") ;
if (lado > 0) then
    area = lado * lado;
    printf ("A área do quadrado é: %f",area);
else
    disp("Não é um valor válido")
end


